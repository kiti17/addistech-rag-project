# Deployment (Optional)

If you deploy this app (e.g., Render/Railway), paste the public URL here.

Example Start Command:
```
uvicorn app.main:app --host 0.0.0.0 --port $PORT
```

Required environment variables (if using an LLM):
- LLM_API_KEY
- LLM_BASE_URL
- LLM_MODEL
